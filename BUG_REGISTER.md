# BUG REGISTER — Complete System Bug & Remediation Ledger

| ID | Severity | Module | Bug | Root Cause | Fix | Test | Status |
|---|---|---|---|---|---|---|---|
| BUG-001 | P1 - HIGH | AI & Resume Builder | Certification Recommendation functionality missing from builder and AI runtime | In commit `f434b90`, `generate-certifications` was mistakenly stripped from `aiRuntime.js`, `aiService.js`, and `CertificationsStep.jsx` during grounding refactoring | Restored `generate-certifications` in `aiRuntime.js` (prompt, parser, role fallbacks), `aiService.js` (endpoint sets), `CertificationsStep.jsx` (AI panel, quick-add, deduplication), and all 16 locale dictionaries | `node --test tests/certifications-step.test.mjs backend/test/ai-runtime.test.js` | RESOLVED & VERIFIED |
| BUG-002 | P0 - CRITICAL | IAM & Auth | Super Admin user not found / role not mapped in database | Administrator account (`bhaskar.beyond@gmail.com`) was recorded in MariaDB `users` table with `role = 'USER'`, creating a role mismatch with administrative security guards | Synchronized `role = 'SUPER_ADMIN'` in MariaDB `users` table and enhanced `backend/reset-pwd.js` to enforce dual parity between Firebase custom claims and MariaDB users repository | `node --test backend/test/operators-iam.test.js tests/superadmin-control-plane.test.mjs` | RESOLVED & VERIFIED |
| BUG-003 | P1 - HIGH | AI Service Client | `buildAiRequest` throwing `Unsupported AI operation` for `generate-certifications` | `ALLOWED_ENDPOINTS` and `CONSOLIDATED_CONTENT_OPERATIONS` in `src/services/aiService.js` lacked `generate-certifications` | Added `generate-certifications` to allowed and consolidated sets in `src/services/aiService.js` | `node --test tests/ai-client.test.mjs` | RESOLVED & VERIFIED |
| BUG-004 | P2 - MEDIUM | Localization | All 16 locales missing `CertificationsStep.ai.*` dictionary keys | Translations for AI certification recommendation UI were stripped during prior commit | Restored full translation trees for `CertificationsStep.ai` across `de`, `dk`, `en`, `es`, `fr`, `gk`, `hi`, `is`, `it`, `nl`, `no`, `pl`, `pt`, `ro`, `ru`, `se` | `node --test tests/certifications-step.test.mjs` | RESOLVED & VERIFIED |
| BUG-005 | P1 - HIGH | Test Suite | Test assertion drift in `ai-runtime.test.js` asserting `generate-certifications` throws | Prior commit modified `ai-runtime.test.js` to assert `INVALID_AI_RESPONSE` on certification generation | Updated test assertions in `backend/test/ai-runtime.test.js` to verify valid parsing and role-tailored fallback generation | `node --test backend/test/ai-runtime.test.js` | RESOLVED & VERIFIED |
| BUG-006 | P1 - HIGH | Test Suite | Test assertion drift in `tests/certifications-step.test.mjs` asserting absence of AI recommendations | Prior commit added negative regex matching to forbid AI generation in CertificationsStep | Updated `tests/certifications-step.test.mjs` to positively verify AI recommendation functions, fallback integration, batch add, and all 16 locales | `node --test tests/certifications-step.test.mjs` | RESOLVED & VERIFIED |
