# REGRESSION REGISTER — Historical Regression Tracking & Restoration Audit

| Feature | Last Working Commit | Regression Commit | Cause | Restoration | Status |
|---|---|---|---|---|---|
| **AI Certification Recommendation Engine** | `ef99e91` (`9c479ff`) | `f434b90` | Refactoring regression: during introduction of grounded factual prompts, certification suggestions were misclassified as hallucinated credentials rather than career exploration ideas, causing removal of the operation from backend and frontend | Restored `generate-certifications` in `aiRuntime.js` with structured JSON prompt, robust parsing, and 5-domain role fallback suite; reconnected `aiService.js`; restored UI in `CertificationsStep.jsx` with quick-add, "Add All Recommended", deduplication, and all 16 locales | RESTORED & CERTIFIED |
| **Super Admin Dual-Storage Parity** | `8665383` | `f434b90` | Data loading & provisioning drift: Administrator account `bhaskar.beyond@gmail.com` had `role = 'USER'` in MariaDB `users` table while admin authorization required `SUPER_ADMIN` | Synchronized `role = 'SUPER_ADMIN'` for `bhaskar.beyond@gmail.com` in MariaDB `users` table; updated `backend/reset-pwd.js` to synchronize both Firebase Auth custom claims and MariaDB repository | RESTORED & CERTIFIED |
| **AI Client Endpoint Set for Certifications** | `ef99e91` | `f434b90` | Allowlist truncation: `ALLOWED_ENDPOINTS` and `CONSOLIDATED_CONTENT_OPERATIONS` in `src/services/aiService.js` had `generate-certifications` removed | Restored `generate-certifications` in both sets in `src/services/aiService.js` | RESTORED & CERTIFIED |
| **Multilingual AI Certification Copy** | `9c479ff` | `f434b90` | Localization file stripping: `CertificationsStep.ai` dictionary removed across 16 locale JSON files | Restored localized `CertificationsStep.ai.*` trees for all 16 languages (`de`, `dk`, `en`, `es`, `fr`, `gk`, `hi`, `is`, `it`, `nl`, `no`, `pl`, `pt`, `ro`, `ru`, `se`) | RESTORED & CERTIFIED |
| **Certifications Step Unit Test Contract** | `9c479ff` | `f434b90` | Inverted test assertion: `tests/certifications-step.test.mjs` was modified to assert that `generateUserAiContent` was absent | Restored positive verification in `tests/certifications-step.test.mjs` for AI generation, fallbacks, deduplication, batch add, and all 16 locales | RESTORED & CERTIFIED |
