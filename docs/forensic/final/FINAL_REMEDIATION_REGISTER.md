# Remediation Register (Waves 9-12)

| # | Wave | File(s) | Defect | Fix | Verified By |
|---|---|---|---|---|---|
| 1 | 9 | `backend/routes/adminAudit.js` | Blanket `router.use(requirePermission('system.config.read'))` blocked SUPPORT from help-desk and all later-mounted admin sub-routers | Per-route `requirePermission('audit.read')` on the three audit-log routes | 288/288 RBAC matrix |
| 2 | 9 | `backend/security/auth.js` (`requireRecentAdminAuthentication`) | In non-production, middleware called `next()` without enforcing isSuperAdmin(), allowing ADMIN/AUDITOR/SUPPORT to hit SA-only mutation endpoints | Super-admin gate enforced unconditionally; MFA conditional on flag; recent-auth window enforced only in production or explicit flag | 12 previously-failing RBAC tests pass |
| 3 | 9 | `backend/security/policy.js` | `/admin/health*` mapped to `system.config.read` instead of `security.read`; payments/users mutation fell through to default system.config.write | Health → security.read; payments → payments.manage; users → users.update | 288/288 RBAC matrix |
| 4 | 10 | `src/components/admin/Admin.jsx`, `src/components/admin/sidebar/sidebar.jsx` | Admin shell mounted every route unconditionally; sidebar listed every link; SUPPORT/AUDITOR deep-linked to forbidden tabs got broken 403 pages | `RequireTabPerm` route guards; permission-mapped sidebar filtering; role-aware first-accessible-tab redirect | Manual code review + a11y run |
| 5 | 10 | 32 files under `src/` | 98 debug `console.log` statements in production code leaked internal state to browser console | Stripped via `scripts/strip-console-logs.mjs`; 0 remaining | Lint clean; production build passes |
| 6 | 11 | `src/main.jsx`, `src/components/AppShell/AuthenticatedAppShell.jsx`, `src/components/admin/Admin.jsx`, `src/components/Dashboard/DashboardInterviews/DashboardInterviews.jsx` | Skip link `#main-content` had no target on public pages; duplicate `<main id="main-content">` landmarks caused nested/duplicate-main violations | Single document `<main id="main-content">` in main.jsx; shells use `<div>`; DashboardInterviews card `<main>` retagged as `<section>` | axe-core: 0 serious/critical on 8 pages |
| 7 | 12 | `backend/security/policy.js` | `resolveAdminReadPermission` defaulted to `system.config.read`, denying SUPPORT legitimate access to tenants/employer-applications, and leaving AI/email paths over- or under-gated | Added granular mappings: tenants→tenants.read, employer-applications→users.read, /email/admin/deliverability→email.logs.read, /admin/payment-orders,/subscriptions→payments.read, /admin/ai/quota-stats→ai.usage.read, /admin/ai/entitlements→ai.usage.read, /admin/ai/quota-limits→ai.entitlements.manage | 459/459 expanded RBAC matrix |
| 8 | 12 | `backend/routes/email.js` | Three email-admin routes had no explicit `requirePermission` (relied on global policy, which defaulted to system.config.read) | Added `requirePermission('email.logs.read')` on circuit-breaker-status, `requirePermission('system.config.read')` on /admin/settings, `requirePermission('email.template.manage')` on /admin/custom-templates | 459/459 expanded RBAC matrix + 513 backend tests |

All eight defects were proven, fixed, regression-tested, committed, pushed, and remote-SHA verified.
